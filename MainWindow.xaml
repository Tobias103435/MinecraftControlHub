<Window x:Class="MinecraftControlHub.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:components="clr-namespace:MinecraftControlHub.UI.Components"
        Title="Nexora Launcher"
        Height="800" Width="1280"
        MinHeight="600" MinWidth="960"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BrushAppBackground}"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="False"
        StateChanged="MainWindow_StateChanged">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                             CornerRadius="0"
                             GlassFrameThickness="0"
                             ResizeBorderThickness="6"
                             UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <!-- RootBorder's margin gets padded out when maximized so content
         doesn't get clipped under the taskbar/screen edges - a known
         quirk of WindowStyle="None" + WindowChrome. See MainWindow_StateChanged. -->
    <Border x:Name="RootBorder">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ===================== Custom title bar ===================== -->
            <Grid Grid.Row="0" Background="{DynamicResource BrushSidebarBackground}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center"
                            shell:WindowChrome.IsHitTestVisibleInChrome="False">
                    <TextBlock Text="Nexora Launcher"
                               FontFamily="{StaticResource FontDisplay}" FontSize="12.5"
                               Foreground="{DynamicResource BrushTextSecondary}" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="&#x2500;" Style="{StaticResource WindowControlButtonStyle}" Click="MinimizeButton_Click"/>
                    <Button x:Name="RestoreButton" Content="&#x25A1;" Style="{StaticResource WindowControlButtonStyle}" Click="MaximizeRestoreButton_Click"/>
                    <Button Content="&#x2715;" Style="{StaticResource CloseWindowControlButtonStyle}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- ===================== App body ===================== -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="230"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <components:Sidebar x:Name="SidebarControl" Grid.Column="0"/>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <components:TopBar Grid.Row="0"/>

                    <!-- Swapped based on Sidebar.PageSelected - see MainWindow.xaml.cs -->
                    <ContentControl x:Name="MainContent" Grid.Row="1" Background="{DynamicResource BrushAppBackground}"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
